# Mantine LLM docs — fetched fresh at dispatch time via dispatch-agent.sh
# Do not commit; the file is auto-downloaded from https://mantine.dev/llms-full.txt
00-setup/mantine-llms.txt
00-setup/.mantine-llms-etag

# Node
node_modules/
npm-debug.log
package-lock.json


# Logs
logs/*.log
*.log
logs/mcp-jobs.json

# MCP server compiled output — run `npm run build` to regenerate
mantine-architect-mcp/dist/

# Claude Code — local settings and machine-specific config
# settings.local.json: allowed bash commands (accumulated per session, contains local paths)
# launch.json: preview server config (hardcoded Herd/nvm node path)
# mcp-config.json is intentionally tracked — referenced by dispatch-agent.sh for Figma MCP access
.claude/settings.local.json
.claude/launch.json

# Playwright test artefacts
test-results/

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

*storybook.log
storybook-static
